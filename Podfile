# Uncomment the next line to define a global platform for your project
platform :ios, '12'

target 'WasAuto' do
  use_frameworks!

  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'SwiftLint'
  pod 'SCLAlertView'
  
  target 'WasAutoTests' do
	  inherit! :search_paths
  end

end
